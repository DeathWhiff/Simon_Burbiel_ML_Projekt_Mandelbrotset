Das Notebook kann gestartet werden und es wird ein neuer Ordner namens Modelle erstellt, in denen der durchgang gespeichert wird.
Die von mir bereits durchgeführten Durchläufe finden sich unter dem my_dir-Directory und sind aber momentan im ZIP Format.